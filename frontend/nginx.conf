server {
    listen 80;

    # Proxy everything to FastAPI (port 8000) — API + frontend on same origin
    location / {
        proxy_pass http://rag_api:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_read_timeout 120s;
    }
}
